// @flow
type State = {
}

const initialState = {
}

const update = (state: State = initialState, action: Object) => {
  switch (action.type) {
    default:
      return state
  }
}
export default update
